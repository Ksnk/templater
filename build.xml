<?xml version="1.0" encoding="utf-8"?>
<project name="templater" default="build">

    <taskdef name="preprocess" classname="phing.tasks.ext.preprocessor.preprocessTask"/>

    <target name="build" description="build all">
        <preprocess config="config.xml" force="force">
            <param name="dst" value="build"/>
        </preprocess>

    </target>

</project>